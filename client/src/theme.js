import {createMuiTheme} from '@material-ui/core/styles'

const theme = createMuiTheme({
    palette:{
        primary:{
            main:"#764abc",
        },
        default:{
            main:"white",
        }
    },
    background:{
        default:"#fff"
    }
});
export default theme;